/**
 * Вынести переменные в конструктор, чтобы класс был
 * проинициализирован при создании 
 */
class Car {
  private make: string;
  private model: string;
  private year: number;
  private color: string;
  private price: number;
  private mileage: number;
}
