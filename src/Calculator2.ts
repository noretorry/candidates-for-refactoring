/**
 *  interface ICalculator {
      add(a: number, b: number): number;
      multiply(a: number, b: number): number;
    }
*/

function add(a, b) {
  return a + b;
}

class Calculator2 {
  multiply(a, b) {
    return a * b;
  }
}
