
//  Функция уже минимальна и выполняет своё единственное назначение, функция чистая и предсказуемая.
//  Поэтому, никаких изменений и не требуется. Но можно добавить 
//  тип возвращаемого значения для ясности и изменить return на неявный
//  const calculate = (number: number): number => number * number;

const calculate = (number: number) => {
  return number * number;
};
